import styled from 'styled-components';

export const Calendar = styled.div`
  & .card-title {
    margin-top: auto;
    margin-bottom: auto;
  }

  .card-control-wrapper {
    display: flex;
    grid-column-start: 3;
    grid-row-start: 1;
  }
`;
